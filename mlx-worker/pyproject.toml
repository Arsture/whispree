[project]
name = "notmywhisper-mlx-worker"
version = "0.1.0"
description = "mlx-audio STT worker for NotMyWhisper"
requires-python = ">=3.11"
dependencies = [
    "mlx-audio>=0.4.0",
    "soundfile>=0.12.0",
    "numpy>=2.0.0",
]

[tool.uv]
dev-dependencies = []
